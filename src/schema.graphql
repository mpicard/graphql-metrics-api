type Query {
  allOperations: [Operation]
  operation(id: ID!): Operation
  trace(id: ID!): Trace
}

type Operation {
  id: ID
  name: String
  query: String
  traces: [Trace]
  avgDuration: Float
  avgRpm: Float
}

type Trace {
  id: ID
  version: Int
  startTime: String
  endTime: String
  duration: Int
}
